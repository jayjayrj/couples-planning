spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/couples_planning
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true

app:
  jwt:
    secret: minha-chave-super-secreta-com-pelo-menos-32-caracteres
    expiration: 86400000

logging:
  level:
    org.springframework.security: INFO